spring:
  datasource:
    url: jdbc:postgresql://localhost:6462/warehouse_db
    username: smart_home
    password: smart_home
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

warehouse:
  address:
    country: Kazakhstan
    city: Astana
    street: Main street
    house: "1"
    flat: "1"

server:
  port: 0